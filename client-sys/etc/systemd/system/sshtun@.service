[Unit]
Description=Setup a secure tunnel to %I as system
After=network.target

[Service]
ExecStart=/usr/bin/ssh -T -F /etc/ssh/tunnel/%i %i

# Restart every >2 seconds to avoid StartLimitInterval failure
RestartSec=5
Restart=always

[Install]
WantedBy=multi-user.target

